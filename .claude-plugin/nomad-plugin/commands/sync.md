---
description:
  Preview a full sync (pull, then push) without changing anything (dry-run only). Runs nomad sync
  --dry-run. A real sync must be run in a terminal.
disable-model-invocation: true
---

!`nomad sync --dry-run $ARGUMENTS`
