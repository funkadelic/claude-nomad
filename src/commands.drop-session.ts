/**
 * Surgical removal of a contaminated session from the staged tree of
 * `~/claude-nomad/`. RED-gate stub. The GREEN implementation lands in the
 * next commit. See `src/commands.drop-session.test.ts` for the contract.
 */
export function cmdDropSession(_id: string): void {
  throw new Error('cmdDropSession not implemented (RED gate)');
}
